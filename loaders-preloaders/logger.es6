let checkName = (firstName, lastName) => {
  if (firstName !== 'nader' || lastName !== 'dabit') {
    console.log('You are not Nader dabit.');
  } else {
    console.log('You are Nader dabit.');
  }
};

checkName('nader', 'json');
